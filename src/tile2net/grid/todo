reminder about: https://shapely.readthedocs.io/en/latest/reference/shapely.remove_repeated_points.html

We can use something like the majority vote + confidence mask to force the labels of the pixels with low confidence into the same label as their neighbors starting from like 50 pixels away from the edge

add a changelog
we should have vecgrid.prediction and vecgrid.mask
we must dissolve before simplifying

stitch loader should still use threading within a process if a lot of small grid in the big tile
outdir not correctly being set

The user wants the end result
We should delete them by default and only keep it if explicitly passed

leave these as issues:

for val data:
    centroid, epoch, samplers

set up some benchmarking
    how long does it take for our main branch to run this, how long the previous one takes and now this? also memory and disk space?

delete previous files after successful phase
separate predict() and some sort of segmentation setup
 info file. (csv and geojon/parquet for tile info and shapefile) those we had before
 tiles saved as actual png?

 add the functionality to run it for specific classes like only crosswalks

explicit filepath destinations by constructing a stem2path series and

instead of --no-inference we have:

 --download.only
 --segmentation.only
--vectorization.only
--download.force
--segmentation.force
--vectorization.force

preview seems to be very slow?


bs_val=1
Predicting Segmentation Tiles:  73%|███████▎  | 73/100 [00:30<00:09,  2.72 Broadcast.file.grayscale/s]

python -m tile2net --location "40.7053, -74.0111, 40.7119, -73.9900" --name manhattan/unscaled/pad=1 --segmentation.pad 1 --zoom 20 --model.bs_val 2 --vectorization.length 8
--location "boston common" --name "boston common" --segmentation.pad 1 --zoom 20 --model.bs_val 1 --vectorization.length 8 --polygon.simplify 3 --multi_scale_inference --model.extra_scales 0.5,2


python -m tile2net --location "boston common" --name "boston common" --segmentation.pad 0 --zoom 20 --model.bs_val 8 --vectorization.length 8 --polygon.simplify 3

lines and polygons previews take FOREVER!!!


python -m tile2net --location "43.73982934358233, -70.36009908310434, 43.63293463137132, -70.22757630255016 " --name test_vectorization --zoom 20 --model.bs_val 8 --segmentation.pad 0 --vectorization.length 8 --polygon.simplify 2


uv pip install 'imageio[pyav]'

79892 is writing empty files?

why is it saying "loading ingrid.polygnos from" AFTER I saved the concatenated polygons?

INFO       Loading InGrid.polygons from
	/home/arstneio/PycharmProjects/tile2net/src/outdir/maine/polygons/parquet/test_vectorization.parquet
INFO       Saving preview of polygons to
	/home/arstneio/PycharmProjects/tile2net/src/outdir/maine/polygons/preview/test_vectorization.png
INFO       Loading InGrid.polygons from
	/home/arstneio/PycharmProjects/tile2net/src/outdir/maine/polygons/parquet/test_vectorization.parquet

	preview takes forever!
	why does loading happen twice?

just keep default vectorization.length 4

vec.length 8 is 44% faster but time doesn't matter; entire city of portland is 3 minutes
todo: use the del method to print a message to confirm that loader and dataset have been deleted

--location "43.73982934358233, -70.36009908310434, 43.63293463137132, -70.22757630255016 " --name test_vectorization --zoom 20 --model.bs_val 8 --segmentation.pad 0 --vectorization.length 8 --polygon.simplify 2
